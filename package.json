{
  "name": "lyric-door",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build.mjs",
    "build:local": "tsc && vite build",
    "preview": "vite preview",
    "build:pages:snake": "VITE_DEPLOY_TARGET=pages VITE_PAGES_BASE=ultimate-snake pnpm run build:local",
    "build:pages:arc": "VITE_DEPLOY_TARGET=pages VITE_PAGES_BASE=ultimate-brick-breaker VITE_GAME=arc pnpm run build:local",
    "build:pages:dust": "VITE_DEPLOY_TARGET=pages VITE_PAGES_BASE=2d-dust VITE_GAME=twoDDust pnpm run build:local",
    "build:pages": "pnpm run build:pages:snake && pnpm run build:pages:arc && pnpm run build:pages:dust && node scripts/postbuild-pages.mjs",
    "preview:pages": "pnpm run build:pages && (cd dist && python3 -m http.server 4173)",
    "tauri": "bash -lc 'source $HOME/.cargo/env >/dev/null 2>&1 || true; tauri \"$@\"' --",
    "clean:rust": "bash -lc 'source $HOME/.cargo/env && cd src-tauri && cargo clean'",
    "clean:vite": "rm -rf node_modules/.vite dist",
    "clean": "pnpm run clean:vite && pnpm run clean:rust"
  },
  "dependencies": {
    "@babylonjs/core": "^8.50.2",
    "@babylonjs/loaders": "^8.50.2",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
